import { DateGloblizationPipe } from './date-globlization-pipe';

describe('DateGloblizationPipe', () => {
  it('create an instance', () => {
    const pipe = new DateGloblizationPipe();
    expect(pipe).toBeTruthy();
  });
});
